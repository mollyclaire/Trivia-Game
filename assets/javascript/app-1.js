// Code for simpler game if I can't figure out the more challenging one...

$(document).ready(function() {

var correct = 0;
var incorrect = 0;

// User clicks start button

// Questions + answer choices (radio buttons) appear in #questions

// Timer starts

// User can only select one radio button

// User hits submit
    // Displays amount correct and amount incorrect
    // Timer stops

 // Else, the timer runs out
    // Displays amount correct and amount incorrect



});